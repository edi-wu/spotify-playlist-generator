import React from 'react';
// import styled, { keyframes } from 'styled-components';
// import { Link } from 'react-router-dom';

const InputFormPage = () => (
  <div>This is the page where user enters inputs</div>
);

export default InputFormPage;
